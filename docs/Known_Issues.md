# Known Issues & Risks

Living register of **open** known issues, risks, and out-of-scope follow-ups discovered
while implementing tickets. This is the single place to record such findings — do **not**
fix them inline as part of an unrelated ticket (per `CLAUDE.md` §1: report follow-ups
separately, never fix them automatically).

Resolved items are moved to **`Resolved_Issues.md`** (the archive) rather than kept here as
strikethroughs — this file stays focused on what is still open. Where an open item below
still references a closed one, it links into that archive.

## How to update
When a ticket uncovers a risk, bug, sharp edge, or deferred item:
- **Append** a bullet under the right category (most-recent last). Keep existing entries.
- Note **where** it was found (e.g. "Observed during T0007.2 verification") and, when
  useful, a **candidate follow-up**.
- If the issue warrants its own ticket, still log it here and flag it as a follow-up
  ticket in the completion report.

When a ticket **resolves** an item: move its entry (with the resolution note) to
`Resolved_Issues.md` under the matching category, and say so in that ticket's completion
report. If an open item still cross-references the resolved one, leave a short pointer into
the archive instead of the full text.

`Repo_Current_State.md` links here (open issues) and to `Resolved_Issues.md` (closed).

## Open issues

### Config, startup & deployment
- The repository appears to rely on import-time loading in `src/core/config.py` (module-level `settings = load_settings()`), which makes startup sensitive to working directory (the three `config/*.yaml` paths are resolved relative to CWD) and to missing env/config files. Confirmed during the 2026-07-02 pre-deploy audit. Works in the Docker path (`WORKDIR /app`, config `COPY`ed to `/app/config`), but any invocation from another CWD or without env fails at import.
- **Deploy-secret checklist required before any non-local deploy** (confirmed 2026-07-02): `infra/docker-compose.yaml` (the Langfuse stack) ships upstream `CHANGEME` defaults for `SALT`, `ENCRYPTION_KEY`, `NEXTAUTH_SECRET`, Postgres/Redis/ClickHouse/MinIO passwords, and S3 keys — all `${VAR:-default}` env-overridable, safe locally, **unsafe on a public host**. Candidate follow-up (own ticket at deploy time): author a required-env `.env`/secret checklist and set every one before exposing the stack.
- `DATABASE_URL` is required and must be set in the runtime environment before starting the app.
- `config/settings.yaml` is `COPY`ed into the API image, so changing values like `agent.memory.max_messages` requires a `docker compose build --no-cache api` to take effect — a plain `--build` can reuse a cached `COPY config` layer and silently run stale config.
- **[LOW] qwen agent-model pin not yet live-validated against the tool loop** (residual of the resolved `llama-3.3-70b-versatile` retirement fix — background in `Resolved_Issues.md` § Config, startup & deployment): confirm `qwen/qwen3.6-27b` is a current Groq production model ID and that the agent still invokes `query_clean_jobs`/`get_job_details` cleanly **before the T0011.5 baseline run**, so the baseline measures the shipping model. A wrong ID surfaces as a `ChatGroq` runtime error, not a config error. Background: `research/deepeval-sql-agent-eval-planning.md` §11.4/§11.7 (F1). (Note: the T0011.3/T0012.2 live runs since exercised the agent tool loop with this model — see `Resolved_Issues.md` § Evaluation harness — so this residual is largely superseded; confirm the current model ID once more before the baseline.)

### Agent runtime & prompts
> **Deploy-gating note (2026-07-02 audit):** the four model-*behavior* items below — the redundant double tool-call, the out-of-schema stall, the freshness fabrication, and the hidden-salary phrasing — are **model non-determinism, not code defects**. They are re-scoped to the **Evaluation milestone** (measure with a golden Q&A set + Langfuse scoring) and are explicitly **not** deploy-blockers. Do not treat them as bugs to be closed by another round of prompt-tinkering.
>
> **Update 2026-07-03:** the Evaluation milestone is now the **next** milestone (`Tickets.md` T0011), sequenced *before* Ingestion Deploy Readiness (`Tickets.md` T0013, deferred) — precisely because these behaviors, plus the planned `is_active` honesty hedge, must be *measured* before a stage whose honesty guarantees depend on them is built. Decisions captured in `research/deployment-research-plan.md` §4.2.
> **Update 2026-07-05:** Ingestion Deploy Readiness is renumbered T0013; a new `Tickets.md` T0012 (Hardening & Known-Issue Fixes) sits between the Evaluation milestone and it, closing several of the still-open items in this file.

- `load_sql_generation_prompt()` returns a plain `str` rather than a `SystemMessage` like `load_system_prompt()`, since the SQL-generation flow combines it with `load_schema_context()` text before sending it to the model.
- **Residual mypy type-variance (benign, 2026-07-02 audit):** `src/core/checkpointer.py:25` (`AsyncPostgresSaver` connection-pool row-type `tuple` vs `dict`) and `src/agents/runtime/middleware.py:48` (`trim_messages` `list[BaseMessage]` vs the narrower `ModelRequest.override` union) are correct at runtime — the stubs are imprecise. Left un-suppressed so they stay visible; annotate with a commented `# type: ignore[...]` if/when a fully-green `mypy` gate is wanted.
- Observed during T0006.10 verification: the agent sometimes calls `query_clean_jobs` twice in a row with identical arguments before producing its final answer (harmless — deterministic, no side effects — but wastes one round-trip). Candidate follow-up: investigate prompt/loop tuning to remove the redundant call.
- Context trimming caps only what the model sees per turn (`agent.memory.max_messages`, default `20`); the full thread keeps growing in the checkpointer by design. There is no storage-side pruning or token-based budgeting in this MVP (summarization/token budgeting are explicit non-goals).
- Observed during T0007.2 manual verification: asking a refining follow-up about an attribute that has no corresponding column in `clean_jobs` (e.g. "Which of those are remote?" — there is no `remote`/`location` column exposed in the schema context) makes the agent stall for several seconds before it works out it cannot answer, rather than recognizing quickly that the attribute isn't queryable. The eventual answer is still correct/non-fabricated, but the latency suggests the system/SQL-generation prompt could more explicitly guide the model to recognize out-of-schema attributes faster. Candidate follow-up: tune the schema-context or system prompt so the model short-circuits on out-of-schema refinements instead of spending a full reasoning pass figuring it out.
- **`posted_date` intentionally absent from agent schema** (T0009.7): `posted_date` is a column in the `clean_jobs` DDL (added T0009.1) but is never populated by the current ingestion pipeline (VietnamWorks API does not surface a reliable published date). It is deliberately omitted from `config/prompts.yaml` `schema_context` and `system_prompt` so the agent cannot query or reference it. **Verified during T0009.8, with a caveat**: asked the freshness question 3 times against the live stack ("Which of the AI Engineer jobs were posted most recently?" and the ticket's exact wording) — 2 of 3 attempts correctly declined, but 1 of 3 fabricated a specific "most recently posted" job despite having no date data at all. The schema-omission mechanism works (the column is genuinely unreachable via SQL), but the model doesn't reliably refuse to *guess* an answer from title/description context alone. Candidate follow-up: strengthen the honesty-rule wording/few-shot example for freshness specifically, or add a guardrail that detects date-like fabrication in the answer. When a source surfaces reliable publish dates, expose `posted_date` in the agent schema and update honesty rules accordingly.
  - **Reliable-time-column path (design note, 2026-07-04):** the VietnamWorks timestamps that *do* exist (`onlineOn`/`approvedOn`/`expiredOn`, per `research/job-site-comparison.md` §122) are what make a *posting* date unreliable, not their absence: `onlineOn` is a re-listing timestamp that churns whenever an employer refreshes/reposts (it's exactly what powers `order: onlineOn desc` newest-first pulls), `approvedOn` is an administrative approval time, and `expiredOn` is a future expiry — none reliably means "when this job was first posted." The trustworthy alternative is not a source date at all but an **ingestion-owned** timestamp (`first_seen_at`/`last_seen_at`, which we fully control) or an honestly-renamed `listed_on` column (with a `schema_context` description that says "last relisted — may reflect an employer refresh, not original posting"). All three depend on the **accumulate-upsert persistence planned for T0013** — today `clean_jobs` is `TRUNCATE`d and rebuilt every ingestion run, so any owned timestamp would reset to "this run." Deferred as a **T0013 rider, gated on the T0011 baseline**; fold into `research/deployment-research-plan.md` §4 when T0013 is drafted.
- **Hidden-salary honesty rule not reliably followed** (observed during T0009.8): the T0009.7 honesty rule explicitly says "say so plainly rather than claiming it is not in the data," but asking for the salary on a hidden-salary posting (`is_salary_negotiable = true`, `salary_min IS NULL`) reproducibly (2/2 attempts) returned "The salary information ... is not available in the data" — the exact phrasing the rule prohibits. Confirmed this is not a code/formatting bug (the table formatter renders raw column values only; `is_salary_negotiable` is passed through as-is) — it's the model not adhering to the prompt wording. Candidate follow-up: add an explicit few-shot example to `system_prompt` showing the desired negotiable-salary phrasing, or test with a lower-variance model/temperature setting.
- **`id`-in-SQL nudge is best-effort, not guaranteed** (T0009.11): `query_clean_jobs`'s SQL is model-generated, so `id` reaches a list result only if the model actually selects it — enforced via `config/prompts.yaml` `sql_generation` guidance ("always SELECT id as the first column when listing rows"), not deterministically. If the model omits `id` on some turn, `get_job_details` simply has no id to chain from that turn (the tool still degrades gracefully — "No posting found for id X" — for any id it is given, it just won't be given one). This is intentionally not "fixed" by rewriting the model's SQL to force-inject `id`, since that would blur the tool-boundary line between model-generated SQL and deterministic enforcement. Candidate follow-up: if omission proves frequent in practice, consider a few-shot example in `sql_generation` reinforcing the `id`-first convention.

- **[LOW] No deterministic "still open?" golden yet for `listing_expires_on`** (found during T0013.3): the schema and prompt layer now expose `listing_expires_on`, but this ticket deliberately leaves the behavior covered by manual verification only because the eval fixture is all future dates plus NULLs. Candidate follow-up: add a deterministic still-open/expiring-soon golden once the fixture has a stable mix that can assert a meaningful answer.
- **[LOW] `pre-deploy-refinement-plan.md` still has older 13-column / `job_level` hidden references outside §1b** (found during T0013.5): this ticket intentionally reconciled only §1b per scope, leaving surrounding older Phase 0 prose in place even though the frozen contract is now 16 columns and `job_level` is visible. Candidate follow-up: update the remaining plan prose when the eval-baseline/metric-freeze work revisits the plan.
- **[LOW] T0015.1 intentionally leaves the question-bank §12 open-decision list and final G47 wording sign-off unresolved** (found during T0015.1): the behavior spec is now reconciled to the frozen 16-column schema, but the policy choices explicitly reserved for T0015.2 still remain open — especially the "senior roles" title-match rule and the final canonical-phrase wording pass. Candidate follow-up: complete that sign-off in T0015.2 rather than silently settling it in docs-only reconciliation work.
- **[LOW — developer-run verification still needed] T0015.3's live prompt-version surfacing was not exercised end-to-end in this sandbox** (found during T0015.3): the config loader, runtime wiring, and unit tests are green, but the two acceptance checks that need external state — confirming `prompt_version` in Langfuse trace metadata and confirming `evals/test_three_seams.py` prints it during a live `deepeval` run — require real Groq/Langfuse/judge credentials plus the fixture DB. Candidate follow-up: run the new T0015.3 steps in `docs/Manual_Verification_Guide.md` on a fully credentialed developer machine and record the observed result.

### Query tooling & SQL safety
- **[LOW] No "here are the N you asked for — more exist" hint on an honored explicit count** (found during T0010.7): when a user explicitly asks for a specific count (e.g. "top 3") and more than 3 matches actually exist, `resolve_bounds` honors the requested `LIMIT` exactly and the answer reads "Found 3 result(s)" with no indication that additional matches exist beyond the 3 shown — technically correct (3 rows were genuinely returned and displayed) but could read as "there are only 3" to the user. Explicitly out of scope for T0010.7 (non-goal: no such hinting). Candidate follow-up: detect this case (fetch `requested + 1` instead of exactly `requested` when honoring an explicit count) and add a soft hint, e.g. "Showing the 3 you asked for — more may match."

### Data ingestion vocabulary (T0013.1)
- **[LOW] Tech vocabulary refresh cadence is manual** (found during T0013.1): `config/tech_vocabulary.yaml` and `data/vendor/{languages.yml,devicon.json}` are committed snapshots refreshed by `uv run python scripts/build_tech_vocabulary.py`; there is no scheduled refresh or upstream-change review yet. Candidate follow-up: add a lightweight quarterly refresh ticket/checklist that rebuilds the snapshot and reviews diff noise before committing.
- **[LOW] Vocabulary build requires network access** (found during T0013.1): the runtime is offline/deterministic because it reads the committed snapshot, but refreshing the snapshot downloads GitHub Linguist and Devicon raw files. Offline developer machines can run ingestion/tests, but cannot refresh the vocabulary until network access is available. Candidate follow-up: document this in contributor setup or add an explicit `--offline` validation mode if refresh scripting grows.
- **[LOW] Upstream vocabulary contains common English-word ambiguities** (found during T0013.1): extraction is vocabulary-only, but some legitimate language/tool names are common words in job prose. `BASIC` is denylisted after the ticket spot-check showed `SQL - basic` was a false positive. Candidate follow-up: review future false positives and keep the denylist small and evidence-based.
- **[LOW] Vietnamese alias coverage is intentionally small** (found during T0013.1): `config/tech_seed.yaml` includes only the frequent VI aliases surfaced by the 112-record VietnamWorks audit, not a full multilingual skills layer. Candidate follow-up: if VI tags remain a major coverage gap, evaluate ESCO/O*NET or a curated VI alias expansion as its own ticket.

### Evaluation harness (T0011.1)
- **Judge selected: Groq `openai/gpt-oss-120b`** (spike run 2026-07-03, `scripts/eval_judge_spike.py`): both Groq candidates named in `research/deepeval-sql-agent-eval-planning.md` §11.4 returned schema-valid JSON on a live `GEval` probe — `openai/gpt-oss-120b` PASS, `qwen/qwen3.6-27b` PASS. Per the ticket's priority order (first candidate that passes wins), `openai/gpt-oss-120b` was picked; `qwen/qwen3.6-27b` is the proven fallback. This feeds the F1 agent-migration follow-up above (same model family, but the agent's own tool-calling loop must still be validated independently — the judge only exercises GEval's chain-of-thought→JSON path, not tool calls). **Superseded:** the judge later moved to Google `gemini-2.5-flash` in T0011.6 (see `Resolved_Issues.md` § Evaluation harness (T0011.3)) — this note is retained for the F1 background and the Groq fallback record.
- **`deepeval test run` crashes on Windows unless UTF-8 output is forced** (found 2026-07-03 building the scaffold test): the `rich`-based CLI output prints a `⚠` warning glyph, which crashes with `UnicodeEncodeError: 'charmap' codec can't encode character '⚠'` under the default Windows `cp1252` console encoding. Workaround: run with `PYTHONUTF8=1` (e.g. `PYTHONUTF8=1 uv run deepeval test run evals/test_judge_scaffold.py`) — not a bug in our code, purely a Windows-console/`rich` interaction. Candidate follow-up: document this env-var requirement wherever `deepeval test run` is invoked (dev docs, and any future CI step, though CI runners are typically UTF-8 by default so this is Windows-dev-only).

### Evaluation harness (T0011.2)
- **Ticket asked for two files that can't coexist: `evals/goldens/__init__.py` and `evals/goldens.py`** (found while implementing T0011.2): the ticket's "Allowed areas" list names both a package `evals/goldens/` (holding `golden_dataset.json` + `__init__.py`) and a sibling module `evals/goldens.py`, but Python cannot resolve `import evals.goldens` to both a same-named module file and a same-named package directory at once — one would silently shadow the other. Resolved by putting `load_goldens()`/`build_eval_dataset()` directly in `evals/goldens/__init__.py` and not creating a separate `evals/goldens.py`; `from evals.goldens import load_goldens, build_eval_dataset` works as intended. No functionality lost, just a different file layout than the ticket's literal file list. Flagging in case a later ticket (T0011.3+) assumes `evals/goldens.py` exists as a distinct file.
- **`evals/fixtures/test_fixture_counts.py` needs a live `internhunter_eval` Postgres to do anything** (by design, T0011.2): the 7 count-assertion tests call `load_fixture()` in a module-scoped fixture and `pytest.skip(...)` cleanly if the eval DB (port `5433`) isn't reachable (verified: skips with a clear `ConnectionTimeout` message when Postgres is down, all 7 pass once `docker compose up -d` brings the DB up). This means CI (once stood up, F2) must have the `internhunter_eval`-capable Postgres available for these tests to actually assert anything rather than silently skip. Candidate follow-up: when the CI gate (fast-follow ticket, mentioned in `Tickets.md` T0011.5) is authored, make sure it provisions/seeds `internhunter_eval` rather than relying on the skip path.

### Evaluation harness (T0011.3)
- **[LOW] eval `GEval` criteria are hardcoded in `evals/harness.py`, not in `config/`** — a deviation from the "parameters/prompts live in config" convention (agent prompts are in `config/prompts.yaml`; judge model/rpm in `config/settings.yaml`). Left as-is to match the two pre-existing GEvals (`"SQL Schema Quality"` / `"Honesty"`) and avoid scope creep; centralize eval criteria into config only if eval prompts proliferate. (Context: the two broken premade metrics this GEval work replaced are in `Resolved_Issues.md` § Evaluation harness (T0011.3), T0012.3.)
- **[LOW] Full 17-golden `deepeval test run` was not exercised end-to-end in this sandbox** (2026-07-04): the agent (`qwen/qwen3.6-27b`) and the judge share a rate/token budget — a live spot-check of 5 goldens alone hit a `429 RateLimitError` mid-run. The harness mechanism itself was verified correct: (1) via a fake-model, no-network unit check proving the config-forwarded `generate_sql` call produces its own distinct span, and (2) via multiple live single-golden runs (A1, A3, C3, D1, D2) that returned real, distinct per-seam scores (e.g. seam-1 Tool Correctness `1.0`, seam-2 SQL Schema Quality GEval `0.2`–`0.5`, seam-3 Faithfulness/Honesty `1.0` on D1/D2). Running the **full** golden set live needs either a paid Groq tier, `-n 1` plus inter-case delay, or `-c` cache reuse across retries. Candidate follow-up: budget/schedule a full live run before T0011.5 calibrates thresholds off it.
  - The judge-side double-Groq-load and its rate limit were addressed (judge moved to Google Gemini + a client-side RPM throttle) — see `Resolved_Issues.md` § Evaluation harness (T0011.3).
- **The nested `generate_sql` LLM span is a *sibling* of the `query_clean_jobs` tool span, not its child** (confirmed while building `evals/harness.py`): LangChain's `@tool` config-injection (`langchain_core/tools/base.py`) hands the tool function the **parent-scoped** `RunnableConfig` (the tool node's own config), not the `child_config` it builds internally for its own callback bookkeeping — so forwarding that config into `model.invoke(..., config=config)` attaches the nested LLM call under the *same parent* as the tool span (both children of the LangGraph "tools" node run), not nested one level deeper as `parent_run_id`-based intuition might suggest. `evals/harness.py::_extract_sql_span` locates it by `parentUuid` equality with the tool span's own parent, not the tool span's `uuid`. Not a defect — just a LangChain implementation detail worth remembering if anyone later tries to walk the trace tree assuming strict tool→child nesting.

### Evaluation harness (T0011.6)
- **[LOW] `deepeval`'s Windows console crash (`UnicodeEncodeError` on a `🎯`/`⚠` glyph) still reproduces via `assert_test`, not just `deepeval test run`** (found 2026-07-05, same root cause as the T0011.1 entry above): `evals/test_judge_scaffold.py::test_judge_scaffold` calls `deepeval.assert_test`, whose `rich`-based progress bar crashes on Windows' default `cp1252` console encoding when a raw `-v -s` traceback prints an emoji glyph mid-run. Same workaround applies: run with `PYTHONUTF8=1`. Not re-fixed here since it predates and is out of scope for this ticket.
- **[HIGH — blocks the judge-agreement gate, not this ticket's code] Groq's daily token quota (TPD) for the agent model was already exhausted in this sandbox** (found 2026-07-05 attempting the ticket's acceptance-critical judge-agreement comparison): `qwen/qwen3.6-27b`'s 200,000 TPD budget read `199305/200000 used` (`groq.RateLimitError`, "Please try again in 11m6s") on the very first of 5 planned live agent calls — exhausted by this session's own prior live testing (T0011.1–T0011.5 verification), not by this ticket's changes. **The judge-agreement gate specified in `Tickets.md` T0011.6** ("score ~5 goldens including 2–3 honesty probes with both `gpt-oss-120b` and `gemini-2.5-flash`, confirm the verdicts broadly agree") **could not be completed in this session** — the agent side of the comparison (which must stay on Groq per this ticket's non-goals) has no remaining daily budget. Candidate follow-up (must happen before T0011.5's baseline trusts the Gemini judge): re-run the comparison once the Groq TPD window resets — score cases A1, A3 (non-honesty) plus C1 (freshness), C3 (COBOL/no-match), C5 (hidden-salary) — the three honesty probes already flagged as model-behavior risk areas — with both judges on the same seam-3 `LLMTestCase`s and compare `TaskCompletionMetric`/`FaithfulnessMetric`/`Honesty GEval` scores.

### Evaluation harness (T0012.7)
- **[LOW] `evals/conftest.py`'s `DATABASE_URL` redirect still fires during collection even though the live eval tests are now deselected** (found 2026-07-06): pytest imports every `conftest.py` under the collection root before applying `-m` marker filters, so `evals/conftest.py` still overwrites `DATABASE_URL` to the fixture DB in the environment for the duration of a plain `uv run pytest` run, regardless of the new `eval` marker. Not observed to cause test failures today (no `tests/` test reads `DATABASE_URL` after collection-time import), but it's a latent cross-talk risk if a future non-eval test relies on the real `DATABASE_URL` value. Pre-existing behavior (the evals directory was already collected before this ticket); out of scope for T0012.7 per its ticket instructions. Candidate follow-up: scope the `DATABASE_URL` override inside a fixture rather than module-level `conftest.py` code, or gate it on the `eval` marker being requested.
- **[LOW] `deepeval test run <file>` alone does not select `eval`-marked tests — `-m eval` must be passed through explicitly** (found 2026-07-06): `deepeval test run` invokes pytest under the hood and inherits `pyproject.toml`'s `addopts = "-m 'not eval' --strict-markers"`, so `PYTHONUTF8=1 uv run deepeval test run evals/test_three_seams.py` alone reports "No test cases found, please try again" (0 selected). The verified working form forwards the marker override as a trailing pytest arg: `PYTHONUTF8=1 uv run deepeval test run evals/test_three_seams.py -m eval` (confirmed live against `evals/test_judge_scaffold.py` — the test was selected and executed, failing only on missing judge credentials, not deselection). Documented in `Manual_Verification_Guide.md` → T0012.7 and `Repo_Current_State.md` → Available scripts.

### Evaluation harness — cost & rate-limit exposure (analysis 2026-07-07)
- **[MED] A full 17-golden run fires ~120 sequential Gemini judge calls (~325K judge tokens, ~15 min at `rpm: 8`) — the binding constraint is Gemini free-tier RPD (250–1500) and wall-clock, not TPM.** Full analysis in **`research/eval-cost-and-rate-limits.md`** (token/dollar-equivalent breakdown, provider limits, ranked reduction levers). Actual money cost is **$0** (judge on Google free tier, agent on Groq free tier); paid-equivalent ≈ $0.50/run, ~90% output-driven because `gemini-2.5-flash` is a thinking model. Ranked candidate follow-ups (each its own ticket): (1) cap/disable the judge's `thinking_budget` in `evals/judge.py` — biggest lever, zero metric loss, also improves JSON reliability; (2) drop the redundant seam-3 `FaithfulnessMetric` (overlaps `GEval("Honesty")`), removing ~1/3 of judge calls; (3) pre-supply `evaluation_steps` to the GEvals instead of `criteria` to skip the per-metric step-generation call (dovetails with the T0011.3 LOW item — move criteria into `config/`). Do **not** trim input context/`max_rows` — input is ~15% of cost and TPM is not the bottleneck. Cross-refs the Groq-TPD exhaustion risk in the T0011.6 entry above (the agent side of a full run is separately budget-constrained). **Levers (1) + (2) are being taken now as T0012.10; (3) and a config-driven metric-selection toggle are deliberately deferred** (lever 3 reopens the T0011.3 criteria-in-config trade-off; a runtime metric toggle would make the baseline's metric set mutable, undermining v1 reproducibility).
- **[PLANNED — v2 refinement] The v1 baseline measures *current* agent behavior with a lean, mostly-premade metric set; it does not yet encode the *preferred* behavior.** Once the preferred per-scenario agent behaviors are designed and driven in via **prompt** refinement, revisit **both** (a) the agent prompts that produce those behaviors and (b) the eval prompt/metric choices — the `GEval` criteria wording and which metrics run — so the harness scores against the intended behavior, producing a distinct **Evaluation v2 baseline** (per T0011.5's v1/v2 framing). This is expected, not a defect: T0011 exists to measure, not to fix. Captured 2026-07-07 while scoping T0012.10.

### Evaluation harness (T0012.10)
- **[LOW — BLOCKED, needs maintainer creds] The acceptance-critical live judge-agreement spot-check (thinking-capped vs. pre-cap verdicts on 2–3 goldens incl. an honesty probe) could not be run in the coder sandbox** (2026-07-07): the sandbox has neither `GOOGLE_API_KEY` nor Groq creds set, so `PYTHONUTF8=1 uv run deepeval test run evals/test_three_seams.py -m eval` cannot execute. The thinking-budget cap and `FaithfulnessMetric` removal are implemented and covered by an offline unit test (`evals/test_judge.py`), and `uv run pytest -q` is green, but the "does capping thinking silently weaken the flash judge on subtle honesty calls" question from `research/eval-cost-and-rate-limits.md` §4 is unverified pending the maintainer running the spot-check with real credentials. Follow-up: maintainer runs the T0012.10 step 4 checklist in `Manual_Verification_Guide.md` and records the before/after verdict comparison.
- **[OPEN DECISION] `eval.judge.thinking_budget: 0` (thinking fully disabled) is the current default, but whether to keep it at `0` or allow a nonzero budget is still under consideration** — pending the blocked spot-check above. If the before/after verdict comparison shows the flash judge materially diverges on subtle honesty calls (C1/C3/C5) with thinking off, the default should move to a small nonzero budget (a partial cost win over unlimited thinking, still short of lever (1)'s full savings) rather than `0`. Revisit this value once the spot-check runs; do not treat `0` as final.

### Repo state & version control
- **[LOW] `evals/` colocates its own tests (`evals/test_*.py`, `evals/fixtures/test_fixture_counts.py`) instead of living under `tests/`** (found 2026-07-11, repo-hygiene review): every other package's tests mirror it 1:1 under `tests/` (`tests/services/ingestion`, `tests/agents/runtime`, etc.), but `evals/` breaks that convention by keeping its tests alongside its own source (`evals/goldens.py`, `evals/judge.py`, `evals/harness.py`, `evals/writeback.py`). Not accidental clutter — `evals/conftest.py` redirects `DATABASE_URL` to the fixture DB, and pytest scopes `conftest.py` to its directory and descendants, so keeping the eval tests inside `evals/` prevents that redirect from ever leaking into `tests/`. Still, it means two places to check for "where are the tests." Candidate follow-up: move `evals/test_*.py` + `evals/fixtures/test_fixture_counts.py` + `evals/conftest.py` to `tests/evals/` (preserving the conftest's directory-scoped `DATABASE_URL` redirect) for consistency with the rest of the repo, or explicitly document `evals/` as a deliberate self-contained-subsystem exception in `Repo_Current_State.md` if left as-is.

## Resolved
Resolved issues have moved to **`Resolved_Issues.md`**. See that file for the full
resolution records (fixes, verification, and cross-references), organized under the same
category headers used above.
