## Summary of changes

- Added a fixture-backed Vietnamese prompt spike that keeps production prompts unchanged and records
  the required evidence per run.
- Added the live research record and a pending recommendation template for the measured A1/A2
  decision.

## Files created, changed, or modified

- `scripts/vietnamese_prompt_spike.py`.
- `research/vietnamese-prompt-spike.md`.
- `docs/entries/T0032.4_vietnamese_prompt_spike.md`.

## Commands executed/run

- `uv run python -m py_compile scripts/vietnamese_prompt_spike.py`.
- `uv run python scripts/vietnamese_prompt_spike.py --help`.
- `uv run python scripts/docs_lint.py`.
- `git diff --check`.
- `uv run python scripts/vietnamese_prompt_spike.py --arm A0 --output <temporary path>`.
- `uv run python scripts/vietnamese_prompt_spike.py --arm A1 --runs 3 --output <temporary path>`.
- `uv run python scripts/vietnamese_prompt_spike.py --arm A2 --runs 3 --output <temporary path>`.
- `uv run python scripts/vietnamese_prompt_spike.py --arm A3 --winner A1 --runs 3`.
- `uv run python scripts/vietnamese_prompt_spike.py --arm A4 --winner A1 --runs 3`.

## Build and test results

- Python compilation and the command help output passed.
- `git diff --check` passed.
- Documentation lint reported and then cleared ticket-owned formatting issues.
- Documentation lint still reports the pre-existing stale generated region in
  `docs/Repo_Current_State.md`, which is frozen to ticket agents.
- All five live arms completed against the fixture database and DeepSeek.
- The 72 captured rows select A1 over A2 because A1 preserves canonical `Hanoi` SQL literals.
- The full suite, with inert Langfuse test values, produced 527 passed, 2 skipped, and 2 failures.
- Both failures assert that the frozen `docs/Repo_Current_State.md` generated region is stale.

## Manual verification steps

- Start the fixture database.
- Run `uv run python scripts/vietnamese_prompt_spike.py --arm A0` and inspect its English control
  rows.
- Run A1 and A2 three times, hand-review the honesty rows, and record the winner or non-separation
  in `research/vietnamese-prompt-spike.md`.
- Run A3 and A4 with that winner and inspect the generated SQL arrays for canonical English role and
  location literals.

## Risks

- The selected language policy is evidence for a later promotion ticket, not a shipped change.
- Multi-turn tool following remains unreliable after turn six.
- The strict English-fragment rule needs an explicit source-value exemption decision.
- The automated English-fragment detector is conservative and requires reviewer confirmation.

## Follow-up tickets

- A later Vietnamese-language milestone should promote A1 with a defined source-value language
  policy.
- A follow-up behavior ticket should address application-deadline honesty and the multi-turn tool
  failures found in A3.

## Docs that need updating

- The integration step should publish this entry after the ticket is accepted.

## Known issues

- A live behavioral run requires the fixture database and provider budget.
- The current branch has a stale generated `docs/Repo_Current_State.md` region.
  Only the integration step may regenerate that frozen register.
