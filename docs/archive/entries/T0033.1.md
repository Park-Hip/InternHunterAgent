---
ticket: T0033.1
milestone: M33
title: Promote Vietnamese output language and query vocabulary
status: complete
date: 2026-08-18
goal: Ship the measured A1 Vietnamese output rule and canonical query vocabulary
verified: yes
---

## Summary

Promoted the A1 output-language rule so the agent answers in Vietnamese without translating
canonical database values or source-provided values.
Added Vietnamese accented location and role mappings to the SQL-generation prompt.
The mappings use the existing ingestion vocabulary as the source of truth.
Added bilingual free-text guidance for Vietnamese and English description searches.
Bumped `prompt_version` from `v3` to `v4`.
Re-ran the required A0 control at three runs before implementation.
All three absent-field honesty answers stated that application deadlines are not captured and did
not invent a deadline.

## Files

- Modified `config/prompts.yaml`.
- Modified `tests/agents/runtime/test_prompts.py` to assert the new prompt version.
- Modified `docs/roadmap.yaml` to declare the prompt-version test and roadmap scope needed by this
  ticket.
- Added `docs/entries/T0033.1.md`.

## Commands

- `docker compose up -d`
- `Test-NetConnection localhost -Port 5433`
- `uv run python scripts/vietnamese_prompt_spike.py --arm A0 --runs 3 --output
  $env:TEMP\\internhunter-m33-a0.json`
- `uv run pytest -q tests/agents/runtime/test_prompts.py tests/test_prompt_surface.py
  tests/test_prompt_consistency.py`
- `python scripts/docs_lint.py --check scope --check frozen --check registry`
- `git diff --check`

## Build and test results

The Docker Postgres service reported healthy and port 5433 was reachable.
The A0 control completed 18 probes across three runs.
The targeted prompt suite passed with 28 tests and 41 subtests.
The scope, frozen-register, and registry checks passed.
The full test suite passed 555 tests and skipped 2 tests.
Two documentation-build tests fail until the integration session folds this entry into the frozen
generated registers.

## Manual verification

1. Start Docker Desktop and run `docker compose up -d`.
2. Confirm `docker compose ps` reports the Postgres service as healthy.
3. Run `uv run python scripts/vietnamese_prompt_spike.py --arm A1 --runs 3` and review the answer
   prose for Vietnamese output.
4. Ask for Data Engineer jobs in `Hà Nội` and confirm generated SQL uses the canonical `Hanoi`
   value.
5. Ask about remote work in Vietnamese and confirm the generated query considers both Vietnamese
   and English description terms.
6. Confirm database values such as role names, company names, and locations remain verbatim in the
   answer.

## Risks

The vocabulary is prompt guidance rather than deterministic normalization.
An evaluation ticket must measure accented input and canonical SQL behavior.
The free-text bilingual examples do not solve accent-insensitive matching when neither language
form occurs in the source text.
The output rule increases the prompt surface and can affect the pending Vietnamese glossary and
evaluation captures.

## Follow-up tickets

T0033.2 should translate the 18 behavior glossary values after this prompt contract lands.
T0033.3 should add Vietnamese scenarios, row-aware purity grading, and accented-input probes.

## Known issues

No new known issue was found in this ticket.

## Docs that need updating

The integration session should fold this entry into the frozen completion and manual-verification
registers.
The integration session should regenerate the repository snapshot after merging the ticket.
