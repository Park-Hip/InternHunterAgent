---
ticket: T0034.1
milestone: M34
title: Reproduce the serving memory-window eviction boundary
status: complete
date: 2026-08-18
goal: Establish an English reproduction before selecting a memory-window remedy
verified: yes
---

## Plan

Reproduce the serving context-window eviction with the real asynchronous agent graph and a
recording model.
Keep the runtime policy unchanged because T0034.2 owns selecting its remedy.

## Summary

Added deterministic agent-boundary tests for the message-count trimming policy.
The one-tool path evicts the first turn from the model's input when the sixth turn begins.
The sequential two-tool path evicts the first turn from the final model call in turn four.
The tests prove that the defect is independent of Vietnamese output and provider variation.

## Files

- Modified `tests/agents/runtime/test_memory.py`.
- Added `docs/entries/T0034.1.md`.

## Commands

- `$env:LANGFUSE_PUBLIC_KEY='test'; $env:LANGFUSE_SECRET_KEY='test'`
- `uv run pytest tests/agents/runtime/test_memory.py tests/agents/runtime/test_trimming.py -q`
- `uv run pytest -q`
- `uv run ruff check .`
- `uv run mypy src`
- `uv run python scripts/docs_build.py`
- `uv run python scripts/docs_build.py --check`
- `uv run python scripts/docs_lint.py`
- `git diff --check`

## Build and test

| Command | Result |
|---|---|
| Focused memory and trimming tests | 15 passed |
| Ruff | Passed |
| Mypy | Passed for 44 source files |
| Full non-evaluation pytest suite | Timed out after 124 seconds without a result |
| Documentation checks | Passed after generator rendering |

## Manual verification

- Run the focused test command with non-production placeholder Langfuse keys.
- Confirm the one-tool test omits turn 1 and retains turns 2 through 6 at turn 6's first model call.
- Confirm the two-tool test omits turn 1 and retains turns 2 through 4 at turn 4's final model call.

## Risks

This ticket intentionally preserves the existing context-window behavior.
The deterministic reproduction does not measure provider-specific tool-selection quality.
The full non-evaluation suite did not complete within the 124-second command limit.

## Follow-ups

- T0034.2 must select and implement a remedy from this acceptance baseline.
- Investigate `KI-2026-08-18-full-pytest-timeout` before relying on a full-suite result.

## Known issues

- `KI-2026-08-17-vietnamese-spike-multiturn` **`[MED - OPEN]` Tool-shaped message history
  evicts referents by message count.**
  - **Found:** The T0034.1 recording-model reproduction confirmed the six-turn one-tool and
    four-turn sequential-two-tool boundaries.
  - **Impact:** A follow-up can lack the antecedent it needs for reliable job-tool selection.
  - **Next:** T0034.2 selects and implements the serving memory-window policy.
  - **History:** The original symptom was observed during the 2026-08-17 T0032.4 DeepSeek spike.
- `KI-2026-08-18-full-pytest-timeout` **`[MED - OPEN]` Full pytest suite timed out.**
  - **Found:** `uv run pytest -q` exceeded the 124-second command limit without reporting a result.
  - **Impact:** The ticket branch lacks a current full-suite verification result.
  - **Next:** Identify the slow or blocked test and restore a bounded full-suite check.
  - **History:** The focused memory and trimming tests completed in 1.43 seconds on 2026-08-18.

## Docs

No further document change is required for T0034.1.
The integration step should publish M34's registry outcome after T0034.2 completes.
