---
ticket: T0038.3
milestone: M38
title: Correct the deterministic evaluation grader
status: complete
date: 2026-08-18
goal: Make replay grading faithful to the behavior contract
verified: partial
---

# T0038.3 completion report

milestone: M38
status: complete
branch: feature/t0038-grader-correctness

## Summary of changes

Implemented M38 Grader Correctness across T0038.1, T0038.2, and T0038.3.
Textual rules now resolve glossary and registry anchors, SQL accuracy uses explicit comparison
modes, honesty answers remain visible when SQL accuracy fails, and the cross-currency detector
handles Markdown line breaks.
The frozen v3 obligation replay was re-graded without a new model capture.

## Files created, changed, or modified

- `config/prompts.yaml`
- `evals/scenarios_v1.yaml`
- `evals/scenarios.py`
- `evals/grader.py`
- `evals/execution_accuracy.py`
- `evals/Instrument_Report.md`
- `evals/replays/t0024.4-v3-obligations.json`
- `tests/evals/test_grader.py`
- `tests/evals/test_execution_accuracy.py`
- `docs/roadmap.yaml`
- `docs/entries/T0038.3.md`

## Commands executed/run

- `uv run pytest -q tests/evals/test_grader.py tests/evals/test_execution_accuracy.py
  tests/evals/test_scenarios.py`
- `docker compose up -d`
- `uv run python -m evals.fixtures.loader`
- `uv run python -m evals.replay`

## Build and test results

The focused evaluation suite passed with 36 tests.
The fixture and replay commands were attempted, but Docker did not become ready before the command
timeout.
The frozen replay was graded offline from its committed execution evidence and produced 15 PASS and
3 FAIL after the corrected rules.

## Specific manual verification steps required

1. Start the fixture with `docker compose up -d`.
2. Run `uv run python -m evals.fixtures.loader`.
3. Run `uv run python -m evals.replay`.
4. Grade `evals/replays/t0024.4-v3-obligations.json` and confirm 15 PASS and 3 FAIL.
5. Confirm the three `HON-CURRENCY-1` turns expose both structural and textual checks.
6. Temporarily replace one registry term with a bare string and confirm `load_scenarios` rejects it.
7. Run `uv run pytest -q` and `python scripts/docs_lint.py`.

## Risks

The v3 calibration set contains only 18 honesty turns.
Its corrected 15/18 result must not be treated as a production accuracy estimate.
The fixture replay remains dependent on Docker availability.

## Follow-up tickets

Re-measure human agreement on a broader v3 capture before enabling the judge tier.
Expand replay coverage after the freezer can accept no-SQL turns.

## Docs that need updating

The integration session must fold this entry into the frozen completion, known-issues, manual
verification, ticket, and repository-state registers.

## Known issues

The Docker-backed replay verification was blocked by the local Docker startup timeout.
The judge tier remains intentionally disabled.
