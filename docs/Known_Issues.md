# Known Issues & Risks

Living register of known issues, risks, and out-of-scope follow-ups discovered while
implementing tickets. This is the single place to record such findings — do **not** fix
them inline as part of an unrelated ticket (per `CLAUDE.md` §1: report follow-ups
separately, never fix them automatically).

## How to update
When a ticket uncovers a risk, bug, sharp edge, or deferred item:
- **Append** a bullet below (most-recent last). Keep existing entries; only remove one
  when it is genuinely resolved, and say so in that ticket's completion report.
- Note **where** it was found (e.g. "Observed during T0007.2 verification") and, when
  useful, a **candidate follow-up**.
- If the issue warrants its own ticket, still log it here and flag it as a follow-up
  ticket in the completion report.

`Repo_Current_State.md` links here instead of duplicating this list.

## Open issues

### Documentation drift
- _(none currently open)_

### Config, startup & deployment
- The repository appears to rely on import-time loading in `src/core/config.py` (module-level `settings = load_settings()`), which makes startup sensitive to working directory (the three `config/*.yaml` paths are resolved relative to CWD) and to missing env/config files. Confirmed during the 2026-07-02 pre-deploy audit. Works in the Docker path (`WORKDIR /app`, config `COPY`ed to `/app/config`), but any invocation from another CWD or without env fails at import.
- `main.py` is only a placeholder (`print("Hello…")`). **Confirmed not deploy-blocking** during the 2026-07-02 audit: `docker/Dockerfile`'s `CMD` runs `uvicorn src.api.app:app` directly and never imports `main.py`, so the stub is dead code, not the entrypoint. Candidate follow-up: delete `main.py` or repurpose it as a thin CLI entrypoint.
- **Deploy-secret checklist required before any non-local deploy** (confirmed 2026-07-02): `infra/docker-compose.yaml` (the Langfuse stack) ships upstream `CHANGEME` defaults for `SALT`, `ENCRYPTION_KEY`, `NEXTAUTH_SECRET`, Postgres/Redis/ClickHouse/MinIO passwords, and S3 keys — all `${VAR:-default}` env-overridable, safe locally, **unsafe on a public host**. Candidate follow-up (own ticket at deploy time): author a required-env `.env`/secret checklist and set every one before exposing the stack.
- `DATABASE_URL` is required and must be set in the runtime environment before starting the app.
- `config/settings.yaml` is `COPY`ed into the API image, so changing values like `agent.memory.max_messages` requires a `docker compose build --no-cache api` to take effect — a plain `--build` can reuse a cached `COPY config` layer and silently run stale config.

### API layer
- `trace_url` is always returned as `None` in `src/agents/service.py` (hardcoded `"trace_url": None` at line 24), so tracing metadata is incomplete. Confirmed 2026-07-02. **Still open** — explicitly out of scope for T0010.1 (tracked as C4).
- **[MED] Empty agent answer still returns a 500; the fallback guard is effectively dead** (2026-07-02 per-module review): T0010.1 added a `FALLBACK_ANSWER` coercion in `service.py` for a `None`/empty `response["answer"]`, but `react_agent._extract_answer` **raises `ValueError`** on empty/unreadable final content rather than returning it. So `runtime.ainvoke` raises before the coercion runs → the exception falls through to the generic `500` in `query.py`, and the `service.py` guard covers a case the runtime cannot produce. Candidate follow-up (finish T0010.1's "graceful answer" half): have `_extract_answer` return an empty/sentinel value instead of raising, or catch it in `service.py`, so a blank agent answer becomes `FALLBACK_ANSWER` (200), not a 500. Detail: `Code_Review_Notes.md` doc-insight 3.

### Agent runtime & prompts
> **Deploy-gating note (2026-07-02 audit):** the four model-*behavior* items below — the redundant double tool-call, the out-of-schema stall, the freshness fabrication, and the hidden-salary phrasing — are **model non-determinism, not code defects**. They are re-scoped to the future **Evaluation milestone** (measure with a golden Q&A set + Langfuse scoring) and are explicitly **not** deploy-blockers. Do not treat them as bugs to be closed by another round of prompt-tinkering.

- `load_sql_generation_prompt()` returns a plain `str` rather than a `SystemMessage` like `load_system_prompt()`, since the SQL-generation flow combines it with `load_schema_context()` text before sending it to the model.
- **`response.content.strip()` assumes string content** (mypy-flagged, 2026-07-02 audit): `src/agents/tools/query_clean_jobs.py:41` calls `.strip()` on `model.invoke(...).content`, whose type is `str | list[...]`. With Groq text replies it is a `str` today, but a list-content reply (structured/tool blocks) would raise `AttributeError`. Latent edge case. Candidate follow-up: coerce non-str content to text before `.strip()`.
- **Residual mypy type-variance (benign, 2026-07-02 audit):** `src/core/checkpointer.py:25` (`AsyncPostgresSaver` connection-pool row-type `tuple` vs `dict`) and `src/agents/runtime/middleware.py:48` (`trim_messages` `list[BaseMessage]` vs the narrower `ModelRequest.override` union) are correct at runtime — the stubs are imprecise. Left un-suppressed so they stay visible; annotate with a commented `# type: ignore[...]` if/when a fully-green `mypy` gate is wanted.
- Observed during T0006.10 verification: the agent sometimes calls `query_clean_jobs` twice in a row with identical arguments before producing its final answer (harmless — deterministic, no side effects — but wastes one round-trip). Candidate follow-up: investigate prompt/loop tuning to remove the redundant call.
- Context trimming caps only what the model sees per turn (`agent.memory.max_messages`, default `20`); the full thread keeps growing in the checkpointer by design. There is no storage-side pruning or token-based budgeting in this MVP (summarization/token budgeting are explicit non-goals).
- Observed during T0007.2 manual verification: asking a refining follow-up about an attribute that has no corresponding column in `clean_jobs` (e.g. "Which of those are remote?" — there is no `remote`/`location` column exposed in the schema context) makes the agent stall for several seconds before it works out it cannot answer, rather than recognizing quickly that the attribute isn't queryable. The eventual answer is still correct/non-fabricated, but the latency suggests the system/SQL-generation prompt could more explicitly guide the model to recognize out-of-schema attributes faster. Candidate follow-up: tune the schema-context or system prompt so the model short-circuits on out-of-schema refinements instead of spending a full reasoning pass figuring it out.
- **`posted_date` intentionally absent from agent schema** (T0009.7): `posted_date` is a column in the `clean_jobs` DDL (added T0009.1) but is never populated by the current ingestion pipeline (VietnamWorks API does not surface a reliable published date). It is deliberately omitted from `config/prompts.yaml` `schema_context` and `system_prompt` so the agent cannot query or reference it. **Verified during T0009.8, with a caveat**: asked the freshness question 3 times against the live stack ("Which of the AI Engineer jobs were posted most recently?" and the ticket's exact wording) — 2 of 3 attempts correctly declined, but 1 of 3 fabricated a specific "most recently posted" job despite having no date data at all. The schema-omission mechanism works (the column is genuinely unreachable via SQL), but the model doesn't reliably refuse to *guess* an answer from title/description context alone. Candidate follow-up: strengthen the honesty-rule wording/few-shot example for freshness specifically, or add a guardrail that detects date-like fabrication in the answer. When a source surfaces reliable publish dates, expose `posted_date` in the agent schema and update honesty rules accordingly.
- **Hidden-salary honesty rule not reliably followed** (observed during T0009.8): the T0009.7 honesty rule explicitly says "say so plainly rather than claiming it is not in the data," but asking for the salary on a hidden-salary posting (`is_salary_negotiable = true`, `salary_min IS NULL`) reproducibly (2/2 attempts) returned "The salary information ... is not available in the data" — the exact phrasing the rule prohibits. Confirmed this is not a code/formatting bug (the table formatter renders raw column values only; `is_salary_negotiable` is passed through as-is) — it's the model not adhering to the prompt wording. Candidate follow-up: add an explicit few-shot example to `system_prompt` showing the desired negotiable-salary phrasing, or test with a lower-variance model/temperature setting.
- **`id`-in-SQL nudge is best-effort, not guaranteed** (T0009.11): `query_clean_jobs`'s SQL is model-generated, so `id` reaches a list result only if the model actually selects it — enforced via `config/prompts.yaml` `sql_generation` guidance ("always SELECT id as the first column when listing rows"), not deterministically. If the model omits `id` on some turn, `get_job_details` simply has no id to chain from that turn (the tool still degrades gracefully — "No posting found for id X" — for any id it is given, it just won't be given one). This is intentionally not "fixed" by rewriting the model's SQL to force-inject `id`, since that would blur the tool-boundary line between model-generated SQL and deterministic enforcement. Candidate follow-up: if omission proves frequent in practice, consider a few-shot example in `sql_generation` reinforcing the `id`-first convention.

### Data & ingestion / database schema
- ~~**[MED] `normalize_location` only matches on an exact full-string lookup**~~ **Fixed** (2026-07-02): see § Resolved for the T0010.6 fix. Detail: `Code_Review_Notes.md` bug 6.
- ~~**[LOW/latent] `replace_clean_jobs` would crash on intra-batch duplicate keys**~~ **Fixed 2026-07-02**: `INSERT … ON CONFLICT DO UPDATE` errored if the same `(source, external_id)` appeared twice in one batch. `replace_clean_jobs` now dedups `rows` by `(source, external_id)` (last-write-wins) before the insert, so a batch containing the same key twice can no longer crash the refresh, and the returned count reflects the deduped rows written. Verified via `tests/services/ingestion/test_clean_store.py::ReplaceCleanJobsTests::test_intra_batch_duplicate_key_is_deduped_last_wins`. Detail: `Code_Review_Notes.md` bug 8.

### Query tooling & SQL safety
- ~~**[HIGH] SQL validator does not enforce a single table**~~ **Fixed by T0010.3** (2026-07-02): `sql_validator.validate_sql` previously only checked `"clean_jobs" in statement.lower()` (substring presence), so a query that *also* referenced another table passed — e.g. `SELECT * FROM clean_jobs JOIN raw_jobs USING (source, external_id)`. Now masks string-literal contents, then requires every `FROM`/`JOIN` table reference to equal `clean_jobs` and rejects a comma-separated `FROM` list; a bare `SELECT 1` (no table reference) remains valid. String-literal masking was initially scoped to this table check only; the same masking was later reused to fix the denylist keyword false-positive class (bug 4, `Code_Review_Notes.md`, resolved below). Detail: `Code_Review_Notes.md` bug 1.
- ~~**[MED-HIGH] Blocking LLM call on the async event loop**~~ **Fixed by T0010.4** (2026-07-02): `query_clean_jobs` (async) offloads the DB call via `asyncio.to_thread` but ran `generate_sql`'s `model.invoke(...)` synchronously on the loop, so each Groq SQL-gen round-trip blocked every concurrent request and the health probe. Now `sql = await asyncio.to_thread(generate_sql, question)` — scheduling-only change, `generate_sql`'s internals and the generated SQL are unchanged. Detail: `Code_Review_Notes.md` bug 2.
- ~~**[MED] "Showing N of M" can understate the true match count**~~ **Fixed by T0010.5** (2026-07-02): see § Resolved for the fix. Detail: `Code_Review_Notes.md` bug 5.

### Capacity & performance
- ~~**[LOW] Per-request `client.flush()` on the event loop**~~ **Fixed 2026-07-02**: `react_agent.ainvoke` flushed the Langfuse client synchronously on every request — blocking I/O on the async path that stalled concurrent requests. The blocking `client.flush()` is now offloaded via `await asyncio.to_thread(client.flush)`, matching the existing `asyncio.to_thread(...)` pattern in the codebase; per-request flush semantics are preserved, it just no longer blocks the event loop. Detail: `Code_Review_Notes.md` bug 7.

### Repo state & version control
- _(none currently open)_

### Resolved
- ~~**[MED] "Showing N of M" can understate the true match count**~~ **Fixed by T0010.5** (2026-07-02): `table_formatter.format_rows` used to set `row_count = len(rows)` — rows the SQL returned, already capped by whatever `LIMIT` the *model* wrote — so when the model's `LIMIT` was below the real match count, the tool reported "Found N result(s)" as if N were the total and never surfaced a truncation notice. Fixed the Option-A way: the fetch bound is now system-owned, not model-owned. `query_clean_jobs` rewrites the validated SQL via the new `src/services/query/row_bound.py::enforce_fetch_limit` to fetch `agent.query.max_rows + 1` rows regardless of any `LIMIT`/`OFFSET` the model wrote, and `format_rows` uses the +1 row as a truncation sentinel (`TableArtifact.truncated: bool`) instead of trusting `len(rows)` as a true total. The answer now says "Showing the first N results — there are more matches. Narrow your search…" when truncated, and "Found N result(s)" only when N is genuinely the full match count; no exact total is computed for list queries (rejected Option B: a separate `COUNT(*)`). `COUNT(*)`/scalar queries are unaffected — no truncation notice applies. Verified via `tests/services/query/test_row_bound.py`, updated `tests/services/query/test_table_formatter.py`, and updated `tests/agents/tools/test_query_clean_jobs.py`. Detail: `Code_Review_Notes.md` bug 5.
- ~~**[MED] `normalize_location` only matches on an exact full-string lookup**~~ **Fixed by T0010.6** (2026-07-02): `normalize_location` did `city_alias_map.get(lower)` against the whole address string, so a known city embedded in a free-form address (e.g. `"12 Nguyen Hue, District 1, Ho Chi Minh City"`) never canonicalized and fell through to `"Other"` — location quality depended entirely on clean `workingLocations[].cityName`. Now each alias key is matched against every source as a `\b`-anchored (word-boundary), case-insensitive regex, so a city occurring anywhere in the text is recognized while short aliases (`hn`, `hcm`) still cannot match inside unrelated words (`john`, `technology`). Multiple distinct canonical cities are returned in first-appearance order (leftmost match position within a source, `city_alias_map` YAML order as a tiebreak), deduped as before. Verified via new tests in `tests/services/ingestion/test_transform.py::NormalizeLocationTests` (free-form address, short-alias-inside-address, short-alias-false-positive-guard, multi-city order, dedup) plus the existing `test_normalize_vietnamworks.py` location tests. Detail: `Code_Review_Notes.md` bug 6.
- ~~**[MED] Ingestion aborts inconsistently on one bad payload**~~ **Fixed 2026-07-02**: `loader.run_ingestion` now normalizes each posting inside a per-record `try/except`, skipping and logging (`ingestion.normalize_skipped`, with `source`/`external_id`) any payload that fails `to_normalized_job`, so one malformed record can no longer abort the whole normalization pass and leave `clean_jobs` stale relative to `raw_jobs`. The run summary dict now also returns `skipped`, and `main()` logs the completed summary via `logger.info("ingestion.completed", **result)` instead of `print`. Verified via `tests/services/ingestion/test_loader.py::test_malformed_payload_is_skipped_not_fatal` (3-posting batch, 1 malformed → 2 loaded, `skipped == 1`) plus the full suite and `ruff`/`mypy`. Detail: `Code_Review_Notes.md` bug 3. Note: DN-1's broader "transform from `raw_jobs`, not the fetch batch" redesign remains open — this fix only isolates per-record normalization failures.
- ~~**[MED] Denylist matches keywords inside string literals**~~ **Fixed 2026-07-02**: `sql_validator.validate_sql` now masks string-literal contents (`STRING_LITERAL_PATTERN`) before running the denylist token scan, reusing the same masking already used for the table-allowlist check. Legitimate queries with denylisted words inside literals (`... WHERE description ILIKE '%replace%'`, `company_name = 'Merge'`) now pass; a denylisted verb outside a literal is still rejected. Comment-sequence detection still runs on the unmasked statement. Verified via `tests/services/query/test_sql_validator.py` (new cases added). Detail: `Code_Review_Notes.md` bug 4.
- ~~**Unhandled `None` answer collapses into an opaque 500** (C1, mypy-flagged, 2026-07-02 audit)~~ **Fixed by T0010.1**: `src/agents/service.py` now types its return as a local `AgentResponse` `TypedDict` (`answer: str`, not `str | None`) and coerces a `None`/empty/whitespace-only runtime answer into `FALLBACK_ANSWER` ("I couldn't produce an answer for that — please try rephrasing.") before it ever reaches `QueryResponse`. Verified: `uv run mypy` no longer reports the `service.py`/`query.py` null-into-`str` union error; `tests/api/test_query.py::GenerateAgentResponseTests` covers `None` and whitespace-only runtime answers.
- ~~**All exceptions collapse into the same generic 500** (C5, 2026-07-02 audit)~~ **Fixed by T0010.1**: `src/api/routes/query.py` now raises a new `InvalidQueryError` (`src/core/errors.py`) for a blank/whitespace-only `payload.query` and maps it to a `400 "Query must not be empty."` before `generate_agent_response` is ever called; genuine internal failures still fall through to the existing `500 "Failed to process query"` path with the `query.failed` server-side log intact and no internals leaked. Verified via `tests/api/test_query.py::test_query_route_returns_400_for_blank_query` (asserts `generate_agent_response` not awaited) and the existing `test_query_route_returns_500_when_service_fails`.
- ~~**Large uncommitted pile vs. "completed" record** (found 2026-07-02 audit)~~ **Resolved 2026-07-02**: the T0009.3–T0009.11 work plus the pre-deploy audit — previously all uncommitted on top of `199dceb feat(T0009.2)` — were committed as three coherent logical commits on `feature/t0009.11-job-detail-tool`: `efb8ac7 feat(T0009.3-.9): data ingestion pipeline`, `171da99 feat(T0009.10-.11): bounded query output + get_job_details`, `ce68925 chore: pre-deploy audit`. Working tree clean; history now matches the `Repo_Current_State.md` completed-ticket record.
- ~~**Pre-deploy audit batch 1** (2026-07-02): endpoint drift + doc drift + lint + static-analysis setup~~ — (a) **API endpoint drift**: docs said `POST /api/v1/agent/chat` but the route was `/agent/query`; renamed the route to `/agent/chat` (owner decision — "chat" is canonical for the coming UI) and updated `tests/api/test_query.py`, `docs/MVP_Technical_Design.md`, `docs/Manual_Verification_Guide.md`. (b) **Doc drift**: corrected the Langfuse compose path (`infra/langfuse/docker-compose.yaml` → `infra/docker-compose.yaml`) in `Repo_Current_State.md`'s folder tree, reorg note, and command list; added the missing runtime deps (`cloudscraper`, `beautifulsoup4`, `lxml`, `httpx`) and dev deps (`ruff`, `mypy`) to its dependency list; fixed the stale "four columns / planned (T0010)" header in `MVP_Technical_Design.md` §schema-evolution. (c) **Static analysis stood up**: added `ruff` + `mypy` (with the `pydantic.mypy` plugin) as dev deps and `[tool.ruff]`/`[tool.mypy]` config in `pyproject.toml`; `ruff check .` now passes (7 autofixes applied incl. `factory.py`'s unused `settings` import; 2 unused-var tests fixed; `scripts/` spikes excluded); `mypy` dropped from 10 → 3 errors (the 3 residual are triaged above). All 199 tests still pass.
- ~~**Architecture-invariant audit** (2026-07-02)~~ — verified clean by grep: nothing in the request path imports `services.ingestion`; `langfuse` is imported only in `agents/tracing/langfuse.py`; the query DTOs (`TableArtifact`/`QueryToolResult`/`QueryRefusal`) never appear outside `tools/` + `services/query/`; `service.py`/`api/` import no LangChain types. The Full_Design layer laws hold in code.
- ~~**Uncommitted stray edit in `query_clean_jobs.py`** (stray bare `1` after `return _build_answer(table)`)~~ — reverted; file now ends cleanly, verified 2026-07-01.
- ~~**Stray committed file at repo root** (`s -ExecutionPolicy RemoteSigned…Activate.ps1`)~~ — no longer on disk or in `git ls-files`, verified 2026-07-01.
- ~~**Doc drift: `Manual_Verification_Guide.md` pointed at `docker/docker-compose.yaml`**~~ — corrected to the real path `infra/docker-compose.yaml` in both T0003/T0004 steps, 2026-07-01. (The register's own note had also mis-stated the path as `infra/langfuse/docker-compose.yaml`; corrected here too.)
- ~~**Empty-fetch guard relies on source not raising** (T0009.6)~~ **Verified during T0009.8**: `uv run pytest -q tests/services/ingestion/test_clean_store.py` (9 tests) confirms `replace_clean_jobs([])` returns 0 and skips `TRUNCATE`; a code read of `run_ingestion` (`loader.py`) confirms `list(source.fetch())` raises before either `upsert_raw_postings` or `replace_clean_jobs` runs, so a source exception aborts before any DB write. No code change needed — behavior already correct.
- ~~**Live Postgres volume retained a stale pre-T0009 `clean_jobs` schema** (observed during T0009.8 setup)~~ **Addressed by T0009.9**: `scripts/init_db.sql` (`CREATE TABLE IF NOT EXISTS`) intentionally stays non-destructive, so it still can't migrate an existing table with the wrong shape — but the schema-change path is now explicit: `scripts/reset_db.sql` (`DROP TABLE IF EXISTS clean_jobs, raw_jobs CASCADE;` then `\i scripts/init_db.sql`) replaces the old manual `DROP TABLE`. This is an MVP-appropriate reset, not a migration framework, because both tables are fully reproducible (`clean_jobs` is rebuilt every ingestion run, `raw_jobs` is re-fetchable). A full versioned-migration framework (Alembic) remains deliberately deferred — the trigger to revisit is when deployed data becomes genuinely irreplaceable.
- ~~**Large result sets can exceed the Groq free-tier TPM limit** (observed during T0009.8)~~ **Fixed by T0009.10**: `query_clean_jobs` now enforces the bounded-output law deterministically at the tool boundary, independent of the SQL the model generates. `src/services/query/table_formatter.py` drops any `description` column (case-insensitive) from the returned result regardless of what was `SELECT`ed, and caps rows at `agent.query.max_rows` (`20`, added to `config/settings.yaml`, read via a validated loader). When capped, the answer states "Showing N of M ..." with the true total; small/aggregate results (e.g. `COUNT(*)`) pass through unchanged. Full-description retrieval moves to a separate bounded `get_job_details(ids)` fetch (T0009.11, not yet implemented).
